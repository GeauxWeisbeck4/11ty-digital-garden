---
layout: layouts/base-post.njk
section: note
---

<div class="container">

    <h1>{{ title }}</h1>

    <p>
        <small>
            <time datetime="{{ date | machineDate }}">{{ date | readableDate }}</time>
        </small>
    </p>

    {% if tags %}
        <p>
        {% for tag in tags %}
            {%- if tag != "note" -%}
